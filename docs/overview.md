
## Local web UI

A local research UI is available at `/consciousness-forge` via `baseline.web_app`.
Anthropic requests are proxied server-side at `/api/anthropic/messages` and require `ANTHROPIC_API_KEY` on the server.
